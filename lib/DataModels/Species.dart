import 'package:flutter/widgets.dart';

class Species 
{
  String name;
  String speciesType;
  int    animalCount;
  Image  image;

  Species(this.name, this.speciesType, this.animalCount, this.image);
}